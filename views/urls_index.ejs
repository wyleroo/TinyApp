<!DOCTYPE html>
<html lang="en">
<head>
  <title>Wyatt's TinyApp</title>
  <% include ../views/partials/_header.ejs %>
  <br>
</head>
<body>
  <% Object.keys(urls).forEach(function(key){ %>
    <li><%= urls[key] + ' ' + [key] %>
      <form method="POST" action="/urls/<%=key%>/delete">
        <p>
          <button>Delete</button>
        </p>
      </form>
    </li>
  <% }); %>
  <a href="/urls/new">Enter new URL</a>
</body>
</html>

